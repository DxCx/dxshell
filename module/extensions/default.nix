# Extension framework for dxshell.
# To add an extension, create a .nix file in this directory and add it
# to the imports list below. See README.md for the extension pattern.
{...}: {
  imports = [
  ];
}
